# 水硬度滴定终点颜色检测系统 — 开发进度与接口文档

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 开发进度与接口文档 |
| 编制日期 | 2026-08-15 |
| 项目阶段 | 全部功能完成，进入部署准备阶段 ✅ |

---

## 1. 整体进度

| 子系统 | 状态 | 说明 |
| --- | --- | --- |
| 设计文档 | ✅ 已完成 | 需求分析、技术栈说明、数据库设计、schema.sql、图片识别 Demo 说明 |
| Vue 前端 | ✅ 完整 | 登录页 + 学生端 4 页 + 教师端 4 页 + 管理员端 4 页（24 个页面） |
| Java 后端 | ✅ 完成 | 30+ REST API，Service 层完整，H2/MySQL 双模式，JWT + RBAC + AOP 日志 |
| Python 检测端 | ✅ 完成 | 颜色识别 + 30 秒终点状态机 + 后端对接 + 断网缓存 + PySide6 界面 |

---

## 2. Java 后端概览

### 2.1 技术栈

| 组件 | 版本 / 实现 |
| --- | --- |
| Java | 17 |
| Spring Boot | 3.3.5 |
| Spring Security | 6.x（内嵌于 Boot） |
| MyBatis-Plus | 3.5.9 |
| JWT | jjwt 0.12.6 |
| 工具集 | Hutool 5.8.32 |
| API 文档 | springdoc-openapi 2.6.0 |
| 密码加密 | BCrypt |
| 数据库 | H2（开发，内存）/ MySQL 8（生产） |

### 2.2 项目结构

```
backend/
├── pom.xml
└── src/main/java/com/example/titration/
    ├── TitrationApplication.java
    ├── common/
    │   ├── config/
    │   │   ├── MyBatisPlusConfig.java       分页插件
    │   │   ├── MyMetaObjectHandler.java     自动填充 createdAt/updatedAt
    │   │   └── WebMvcConfig.java            CORS 跨域
    │   ├── exception/
    │   │   ├── BusinessException.java       业务异常
    │   │   └── GlobalExceptionHandler.java  全局异常处理
    │   └── result/
    │       └── R.java                       统一响应封装
    ├── security/
    │   ├── JwtAuthenticationFilter.java     JWT 请求过滤器
    │   ├── JwtTokenProvider.java            JWT 生成/解析/校验
    │   └── SecurityConfig.java              Spring Security + RBAC
    └── module/
        ├── auth/                            登录认证
        │   ├── controller/AuthController.java
        │   ├── dto/LoginRequest.java
        │   ├── dto/LoginResponse.java
        │   ├── dto/UserVO.java
        │   └── service/AuthService.java
        ├── user/                            用户管理
        │   ├── controller/UserController.java
        │   ├── controller/ClassController.java
        │   ├── entity/User.java
        │   ├── entity/ClassEntity.java
        │   ├── mapper/UserMapper.java
        │   ├── mapper/ClassMapper.java
        │   ├── service/UserService.java
        │   └── service/ClassService.java
        ├── task/                            实验任务
        │   ├── controller/TaskController.java
        │   ├── entity/ExperimentTask.java
        │   ├── entity/TaskAssignment.java
        │   ├── mapper/ExperimentTaskMapper.java
        │   ├── mapper/TaskAssignmentMapper.java
        │   └── service/TaskService.java
        ├── experiment/                      实验记录
        │   ├── controller/ExperimentController.java
        │   ├── entity/Experiment.java
        │   ├── entity/ExperimentFile.java
        │   ├── entity/ColorSample.java        HSV 采样数据
        │   ├── entity/StateEvent.java         状态事件
        │   ├── mapper/ExperimentMapper.java
        │   ├── mapper/ExperimentFileMapper.java
        │   ├── mapper/ColorSampleMapper.java
        │   ├── mapper/StateEventMapper.java
        │   └── service/ExperimentService.java
        ├── review/                          教师批阅
        │   ├── controller/ReviewController.java
        │   ├── entity/Review.java
        │   ├── mapper/ReviewMapper.java
        │   └── service/ReviewService.java
        ├── threshold/                       HSV 阈值模板
        │   ├── controller/ThresholdController.java
        │   ├── entity/ThresholdTemplate.java
        │   ├── mapper/ThresholdTemplateMapper.java
        │   └── service/ThresholdService.java
        ├── file/                            文件管理
        │   ├── controller/FileController.java
        │   └── service/FileService.java
        ├── statistics/                      数据统计
        │   ├── controller/StatisticsController.java
        │   └── service/StatisticsService.java
        └── log/                             操作日志
            ├── annotation/OperationLog.java 日志注解
            ├── aspect/OperationLogAspect.java  AOP 切面
            ├── controller/OperationLogController.java
            ├── entity/OperationLog.java
            ├── mapper/OperationLogMapper.java
            └── service/OperationLogService.java
```

---

## 3. API 接口清单

### 3.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

| code | 含义 |
| --- | --- |
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未认证 / Token 失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 3.2 认证接口

#### POST /api/auth/login — 用户登录

**Request:**
```json
{
  "username": "student01",
  "password": "123456"
}
```

**Response:**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 3,
    "username": "student01",
    "realName": "张同学",
    "role": "STUDENT",
    "tokenType": "Bearer",
    "expiresIn": 86400000
  }
}
```

#### GET /api/auth/me — 获取当前用户信息

**Headers:** `Authorization: Bearer <token>`

**Response:**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "username": "student01",
    "realName": "张同学",
    "role": "STUDENT",
    "status": "ENABLED",
    "classId": 1,
    "email": "student01@example.com"
  }
}
```

---

### 3.3 实验任务接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/tasks` | 任务列表（分页） | 登录用户 |
| GET | `/api/tasks/{id}` | 任务详情 | 登录用户 |
| POST | `/api/tasks` | 创建任务 | TEACHER / ADMIN |
| PUT | `/api/tasks/{id}` | 更新任务 | TEACHER / ADMIN |
| POST   | `/api/tasks/{id}/assign` | 分配任务给学生 | TEACHER / ADMIN |
| GET    | `/api/tasks/{id}/assignments` | 查看任务分配情况 | 登录用户 |

**GET /api/tasks 查询参数：**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| page | int | 页码，默认 1 |
| size | int | 每页条数，默认 10 |
| status | String | 筛选状态：DRAFT / PUBLISHED / CLOSED |

> 学生角色自动过滤只显示 `PUBLISHED` 状态的任务。

---

### 3.4 实验记录接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/experiments` | 实验记录列表（分页） | 登录用户 |
| GET | `/api/experiments/{id}` | 实验记录详情 | 登录用户 |
| POST | `/api/experiments` | 提交实验记录（保存为草稿 DRAFT） | 登录用户 |
| PUT | `/api/experiments/{id}` | 更新实验记录 | 本人 / TEACHER / ADMIN |
| PUT | `/api/experiments/{id}/submit` | 学生主动提交草稿给教师 | 本人 / TEACHER / ADMIN |
| DELETE | `/api/experiments/{id}` | 删除实验记录 | TEACHER / ADMIN |
| GET | `/api/experiments/{id}/files` | 获取实验关联文件 | 登录用户 |
| GET | `/api/experiments/export` | 导出实验记录 CSV | 登录用户 |
| POST | `/api/experiments/{id}/samples` | 批量保存 HSV 采样数据 | 登录用户 |
| GET | `/api/experiments/{id}/samples` | 获取 HSV 采样数据 | 登录用户 |
| POST | `/api/experiments/{id}/events` | 批量保存状态事件 | 登录用户 |
| GET | `/api/experiments/{id}/events` | 获取状态事件 | 登录用户 |

> 提交状态流转：`DRAFT`（保存草稿）→ `SUBMITTED`（学生提交）→ `REVIEWED`（教师批阅）。
> 教师/管理员查询列表默认排除学生草稿；学生只能查看自己的记录与详情。

**GET /api/experiments 查询参数：**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| page | int | 页码，默认 1 |
| size | int | 每页条数，默认 10 |
| taskId | Long | 按任务筛选 |
| recognitionStatus | String | 按识别状态筛选 |
| submitStatus | String | 按提交状态筛选 |

> 学生角色自动过滤只能查看自己的实验记录。

**POST /api/experiments 请求体：**
```json
{
  "taskId": 1,
  "experimentName": "EDTA 水硬度滴定",
  "sampleName": "自来水样 A",
  "detectMode": "IMAGE",
  "recognitionStatus": "ENDPOINT",
  "recognitionLabel": "滴定终点",
  "matchedColor": "BLUE",
  "confidence": 96.50,
  "hue": 210.00,
  "saturation": 0.4500,
  "brightness": 0.6200,
  "redRatio": 0.0500,
  "purpleRatio": 0.0800,
  "blueRatio": 0.8700,
  "remark": "标准白光环境下完成滴定"
}
```

---

### 3.5 教师批阅接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/reviews` | 批阅列表 | 登录用户 |
| POST | `/api/reviews` | 提交批阅评分 | TEACHER / ADMIN |
| PUT | `/api/reviews/{id}` | 更新批阅 | TEACHER / ADMIN |
| DELETE | `/api/reviews/{id}` | 删除批阅 | ADMIN |

**POST /api/reviews 请求体：**
```json
{
  "experimentId": 1,
  "score": 92.00,
  "comment": "终点判定准确，实验操作规范，数据记录完整"
}
```

> 提交批阅后自动将实验记录的 `submitStatus` 更新为 `REVIEWED`。

---

### 3.6 文件管理接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| POST | `/api/files/upload` | 上传文件（multipart） | 登录用户 |
| GET | `/api/files/download/{id}` | 下载文件 | 登录用户 |
| DELETE | `/api/files/{id}` | 删除文件 | 登录用户 |

**POST /api/files/upload 参数：**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| file | MultipartFile | 文件 |
| experimentId | Long | 所属实验 ID |
| fileType | String | 文件类型：SOURCE_IMAGE / KEYFRAME / VIDEO / REPORT / EXPORT |

---

### 3.7 阈值模板接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/threshold-templates` | 模板列表 | 登录用户 |
| GET | `/api/threshold-templates/default` | 默认模板 | 登录用户 |
| POST | `/api/threshold-templates` | 创建模板 | ADMIN |
| PUT | `/api/threshold-templates/{id}` | 更新模板 | ADMIN |
| DELETE | `/api/threshold-templates/{id}` | 删除模板 | ADMIN |

---

### 3.8 统计接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/statistics/overview` | 教学概览 | TEACHER / ADMIN |

**Response data 字段：**

| 字段 | 说明 |
| --- | --- |
| totalStudents | 学生总数 |
| totalTasks | 已发布任务数 |
| totalExperiments | 总实验记录数 |
| submittedCount | 已提交数 |
| reviewedCount | 已批阅数 |
| pendingReviewCount | 待批阅数 |
| in_progressCount | 滴定进行中 |
| near_endpointCount | 临近终点 |
| endpointCount | 滴定终点 |
| abnormalCount | 颜色异常 |
| averageScore | 平均评分 |

---
### 3.9 用户管理接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/users` | 用户列表（分页） | ADMIN |
| GET | `/api/users/{id}` | 用户详情 | ADMIN |
| POST | `/api/users` | 创建用户 | ADMIN |
| PUT | `/api/users/{id}` | 更新用户 | ADMIN |
| DELETE | `/api/users/{id}` | 删除用户 | ADMIN |
| PUT | `/api/users/{id}/toggle-status` | 启用/禁用用户 | ADMIN |
| PUT | `/api/users/change-password` | 当前用户修改密码 | 登录用户 |

**PUT /api/users/change-password 请求体：**
```json
{
  "oldPassword": "123456",
  "newPassword": "newpass123"
}
```

> 创建用户时 `passwordHash` 字段传入明文密码，后端自动 BCrypt 加密存储。

---
### 3.10 班级管理接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/classes` | 班级列表 | TEACHER / ADMIN |
| GET | `/api/classes/{id}` | 班级详情 | TEACHER / ADMIN |
| POST | `/api/classes` | 创建班级 | ADMIN |
| PUT | `/api/classes/{id}` | 更新班级 | ADMIN |
| DELETE | `/api/classes/{id}` | 删除班级 | ADMIN |

> 删除班级前会检查班级下是否还有学生，有学生则拒绝删除。

---

## 4. 权限模型（RBAC）

| 功能 | STUDENT | TEACHER | ADMIN |
| --- | --- | --- | --- |
| 登录 | ✅ | ✅ | ✅ |
| 查看个人资料 | ✅ | ✅ | ✅ |
| 查看实验任务 | ✅（仅已发布） | ✅ | ✅ |
| 创建/修改/删除任务 | ❌ | ✅ | ✅ |
| 提交实验记录 | ✅ | ✅ | ✅ |
| 查看实验记录 | ✅（仅自己） | ✅ | ✅ |
| 删除实验记录 | ❌ | ✅ | ✅ |
| 上传/下载文件 | ✅ | ✅ | ✅ |
| 批阅评分 | ❌ | ✅ | ✅ |
| 删除批阅 | ❌ | ❌ | ✅ |
| 查看统计 | ❌ | ✅ | ✅ |
| 管理阈值模板 | ❌ | ❌ | ✅ |
| 管理用户 | ❌ | ❌ | ✅ |
| 管理班级 | ❌ | ❌ | ✅ |
| 修改密码 | ✅ | ✅ | ✅ |
| 任务分配 | ❌ | ✅ | ✅ |

---

## 5. 配置说明

### 5.1 数据库双模式

| 模式 | Profile | 数据库 | 启动命令 |
| --- | --- | --- | --- |
| 开发（默认） | 无 | H2 内存数据库 | `.\mvnw.cmd spring-boot:run` |
| 生产 | mysql | MySQL 8 | `.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=mysql` |

### 5.2 application.yml 关键配置（H2 默认）

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:titration_detection;MODE=MySQL
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

jwt:
  secret: titration-detection-system-jwt-secret-key-2026-graduation-project
  expiration: 86400000      # 24 小时

file:
  storage:
    root-path: ./storage    # 文件存储根目录
```

### 5.3 application-mysql.yml 关键配置

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/titration_detection?...&createDatabaseIfNotExist=true
    username: root
    password: root          # 部署时修改
  sql:
    init:
      schema-locations: classpath:schema-mysql.sql
      data-locations: classpath:data-mysql.sql
```

### 5.4 数据库演示账号

| 账号 | 密码 | 角色 |
| --- | --- | --- |
| admin | 123456 | ADMIN |
| teacher01 | 123456 | TEACHER |
| student01 | 123456 | STUDENT |

---

## 6. 当前前端状态

### 6.1 已完成

- Vue 3 + TypeScript + Vite + Element Plus + ECharts 工程骨架
- **MainLayout**：统一布局 + 角色化侧边栏 + 顶部栏修改密码弹窗
- **登录页**：对接后端真实 API，支持三种角色登录
- **学生端**（4 页）：学习概览、实验任务（图片/视频/实时摄像头识别）、实验记录（提交草稿+导出 CSV）、教师反馈
- **学生端实验详情页**：HSV 采样曲线 + 状态时间线 + 关键帧展示
- **教师端**（4 页）：教学概览、任务管理（CRUD+分配）、实验记录+批阅评分+导出 CSV、数据统计
- **管理员端**（4 页）：用户管理（CRUD+启禁用）、班级管理（CRUD）、阈值模板（CRUD）、操作日志
- 颜色识别组件：图片识别（白平衡+自动聚焦+分区采样+色相直方图）、视频识别（逐帧抽帧+ROI）、实时摄像头检测（30 秒终点状态机）
- 角色路由守卫 + Axios HTTP 封装（自动注入 Bearer Token + 401 跳转）
- 11 个 API 模块（auth/task/experiment/review/file/threshold/statistics/user/class/log）

### 6.2 待完成（部署类）

- 数据库从 H2 切换至 MySQL（需先安装 MySQL 服务器）
- Python 检测端打包为 exe
- 视频识别的浏览器编码兼容（H.265 需转 H.264）

---

## 7. 后续计划

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 第一阶段 | Python 视觉检测端原型 | ✅ |
| 第二阶段 | Java 后端平台 | ✅ |
| 第三阶段 | Vue 前端完整页面 | ✅ |
| 第四阶段 | 三端联调 + 打包部署 | 🟡 进行中 |

### 当前状态

三端功能全部完成并联调通过。启动方式：
- 终端 1：`.\mvnw.cmd spring-boot:run -Dspring-boot.run.arguments="--server.port=8080"`（后端 8080）
- 终端 2：`npm run dev`（前端 5173）
- 浏览器：`http://localhost:5173/login`
- 检测端：`detector-client/start.bat` 或 `python main.py`（需先启动后端）

> 注意：环境变量 `SERVER__PORT` 会覆盖后端端口，启动后端需显式指定 `--server.port=8080`。

---

## 8. 附录：演示数据 SQL

`docs/schema.sql` 包含完整的建表语句和以下演示数据：

- 1 个班级：分析化学实验 1 班
- 3 个用户：admin / teacher01 / student01
- 1 个阈值模板：标准白光模板 V1.0
- 1 个实验任务：EDTA 水硬度滴定实验
- 1 个任务分配：student01 → 任务 1

执行方式：
```bash
mysql -u root -p < docs/schema.sql
```
