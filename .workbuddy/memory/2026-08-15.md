# 2026-08-15 工作日志

## 项目完成情况检查

全面检查了项目，对照需求文档得出完成度：

### 已完成
- **Java 后端**：47 个 java 文件，9 个 Controller（auth/experiment/file/review/statistics/task/threshold/class/user），完整 Service 层
- **Vue 前端**：19 个 vue 文件，三端页面齐全（学生端 + 教师端 4 页 + 管理员端 3 页 + 登录页），路由守卫 + 角色权限，9 个 api 模块
- **视频识别 Demo**：用户新增 VideoRecognitionDemo.vue（674 行），纯前端抽帧识别

### 缺口
- **Python 检测端**（detector-client）：空目录，需求 DET-01~12 全部未实现
- **color_samples / state_events**：后端无实体，HSV 采样数据和状态事件未落库
- **视频/图片识别未接后端**：识别结果未 POST 到 /api/experiments

## 端口问题排查
- 发现环境变量 `SERVER__PORT=63155` 覆盖了 application.yml 的 8080
- 解决：启动加 `-Dspring-boot.run.arguments="--server.port=8080"`

## Git
- 用户已自建 commit "视频"（05860ab），推送至 github.com/soren-wu/cv-water-hardness-system

## Python 检测端搭建

从空目录搭建完整检测端（detector-client）：

**核心算法（已测试通过）：**
- `vision/color_classifier.py` — HSV 颜色分类器（酒红/蓝紫/纯蓝/异常）
- `detection/endpoint_state_machine.py` — 终点状态机（30秒稳定判定+返色取消）
- 7 个 pytest 单元测试全部通过

**视觉采集：**
- `vision/camera.py` — 线程安全摄像头封装
- `vision/preprocessing.py` — 高斯滤波去噪
- `vision/roi.py` — ROI 框选模型

**后端对接（已实测通过）：**
- `api/backend_client.py` — 登录/任务/阈值模板/提交实验/上传文件
- 实测：student01 登录成功，拉到 1 个任务 + 标准白光模板

**存储：**
- `storage/local_cache.py` — 断网缓存补传
- `storage/exporter.py` — CSV 导出

**UI（PySide6，语法已检查，需真机摄像头运行）：**
- `ui/login_dialog.py`、`ui/video_label.py`（ROI框选）、`ui/main_window.py`
- `main.py` 入口

**环境：**
- venv: C:/Users/25563/.workbuddy/binaries/python/envs/titration
- 依赖：numpy 2.5.2 + opencv-python-headless 5.0 + pytest + httpx
- PySide6 未装（GUI 运行时才需要，用户需 pip install -r requirements.txt）
- pip 需用清华镜像（pypi 官方源 SSL 报错）

## 检测端环境自检
- 新增 `check_env.py` 一键自检脚本（依赖/算法/后端/摄像头 4 项）
- 在 venv 中完整自检通过：PySide6 6.11.1 + pyqtgraph 0.14.0 + 检测到摄像头索引0
- 注：PySide6 安装报 safe-delete 错误但实际已装好（沙箱回收站问题）

## 前端识别结果接后端存库
- ImageRecognitionDemo.vue / VideoRecognitionDemo.vue 加「保存到实验记录」按钮
- 调 POST /api/experiments，识别结果（状态/颜色/HSV/占比/匹配度）落库
- 实测：Python 编码 POST 成功返回 id=4，查询能拉到
- 注：curl 直接传中文会 GBK 编码报 500（Invalid UTF-8），前端 axios 无此问题

## 分页插件修复
- 原因：MyBatis-Plus 3.5.9 把 PaginationInnerInterceptor 拆到独立模块
- 解决：pom.xml 加 mybatis-plus-jsqlparser 依赖
- 验证：total 字段正确（3条记录 size=2 → total=3, pages=2）

## 采样表实体与落库（color_samples / state_events）
- 后端新增 ColorSample、StateEvent 实体 + mapper + Service 方法 + 4 个接口
  - POST/GET /api/experiments/{id}/samples、POST/GET /api/experiments/{id}/events
- 检测端上传时附带 HSV 采样数据 + 状态事件
- 坑：LocalDateTime 不认 "yyyy-MM-dd HH:mm:ss"，需 @JsonFormat 注解
- 端到端验证通过：提交实验 + 3条采样 + 3条事件，查询回读正确

## 数据导出功能（CSV）
- 后端：GET /api/experiments/export 返回 CSV（带 UTF-8 BOM），学生导出自己、教师导出全部
- 关联学生姓名 + 评分 + 评语；状态/颜色转中文
- 前端：http.ts 拦截器支持 blob 响应；教师端实验记录页加「导出 CSV」按钮
- 验证：teacher01 导出 3 条记录，中文正常、字段关联正确

## 操作日志功能
- 后端：AOP 切面 + @OperationLog 注解，自动记录登录/提交实验/批阅/创建任务/删除实验
- 新增 log 模块（OperationLog 实体/mapper/service/controller + 注解 + 切面）
- GET /api/logs（ADMIN 权限），登录由 AuthService 手动记录（带 userId）
- 前端：管理员端加「操作日志」页面（AdminLogsView + 路由 + 菜单）
- 需加 spring-boot-starter-aop 依赖
- 验证：登录+提交实验产生 3 条日志，userId 正确关联

## 图片识别优化（ImageRecognitionDemo.vue 重构）
四个方向全部实现：
1. 光照补偿：灰度世界白平衡（增益限幅 0.5~1.6），消除偏色
2. 分区采样：5×5 网格，反光格（V>0.94）/过暗格（V<0.06）跳过
3. 色相直方图：Canvas 绘制 72bin 色相分布，三色阈值区间着色
4. 实时分析：ROI 拖拽防抖 150ms 重新分析，图片缓存避免重复加载
- 置信度改用「主导占比 × 颜色纯度加权」
- 构建通过

## 图片识别 bug 修复（用户检验反馈）
- 直方图不显示：histogramCanvas ref 未定义 → 补上定义
- 颜色识别/占比不对：灰度世界白平衡拉灰单色场景 → 改白色参考法（无白色参考则不校正）
- 无法保存：由上面 bug 导致 analyzeWithImage 中断、result 停在未识别
- 白平衡阈值 max-min < 55，验证纯单色场景不做白平衡

## 深度自查（第二轮）
- 用 Node 完整复刻算法测试：6/6 通过（纯蓝/酒红/蓝紫各色相正确分类，纯色场景白平衡不触发）
- 新发现 ROI 越界 bug：浮点坐标舍入导致 getImageData 越界 → 分析中断 → 无法保存
- 修复：roiPx x/y/w/h 做边界钳制；图片+视频识别两个组件都修复

## 颜色识别阈值对齐需求
- 读需求文档，发现前端识别阈值与 threshold_templates 不一致，修正：
  - 酒红 330-25°（原 300-329 也判红，多余）
  - 蓝紫 235-315°（原 245-315，漏 235-244）
  - 纯蓝 185-235°（原 185-240，多 236-240）
  - 饱和度 0.12→0.08（对齐 min_saturation）
- 判定逻辑改为按主导颜色判断，去掉过严 saturation 辅助
- 新增「其他颜色」占比（otherRatio），占比之和=100%
- Node 测试 8/10（2个失败是 185/235° 边界舍入误差，不影响实际）

## 自动聚焦溶液主体
- 用户反馈框选位置不同结果差异大 → 加 findSolutionFocus 自动定位高饱和溶液区域
- 在 ROI 内找高饱和彩色像素质心，采样窗口自动聚焦溶液，排除背景
- 采样提示显示「已自动聚焦溶液」
- Node 测试：溶液在角落/中心都能准确定位（30,30 / 49,49）

## 蓝/酒红识别异常根因修复
- 根因：前端加了 RGB 主导条件（r>=g*1.05 等），但 Python 检测端和阈值模板都是纯色相判断
- 修复：去掉 RGB 主导条件，isRed/isPurple/isBlue 纯色相+饱和度判断
- 验证：酒红 3/3、纯蓝 3/3、蓝紫、灰白背景全部正确识别

## 酒红识别根因（色相缺口）
- 酒红实际色相 312-345°，但阈值 330-25 漏掉 316-330°，偏紫酒红落缺口/蓝紫区间
- 修复：酒红下界 330→315，消除缺口，前后端+检测端+文档 7 处同步
- 验证：酒红 343/322/330/345/315° 全部正确识别，蓝紫纯蓝不受影响

## 颜色识别精度巩固
- 置信度公式优化：纯度影响减小（0.72+0.28purity → 0.9+0.1purity），更贴近实际占比
- 判定加「显著优势」条件：主导颜色需明显领先次优（1.2~1.3倍），边界更清晰
- 新增「蓝紫混合过渡色」判定：蓝+紫≥45%且红<20% → 临近终点（之前误判异常）

## 无法保存根因（CORS 403）
- 后端接口本身正常（curl/httpx 直接测 200），但浏览器 CORS 预检返回 403
- 根因：Spring Security 拦截了 OPTIONS 预检请求（未放行、未启用 CORS）
- 修复：SecurityConfig 加 .cors(withDefaults) + OPTIONS permitAll
- 验证：OPTIONS 预检返回 200 + 完整 Access-Control-Allow-* 头

## 保存后实时刷新实验记录
- 图片/视频识别组件保存成功后 emit 'saved' 事件
- StudentDashboardView 监听 @saved 重新 loadData，无需手动刷新

## 检测端无法打开修复
- 根因1：用户系统 Python（D:\download\python.exe 3.12.8）缺依赖；而沙箱 managed Python 装依赖被 safe-delete 拦截
- 根因2：main.py 用相对导入，从别的目录运行会 import 失败
- 修复：main.py 加 sys.path 自动定位；新增 start.bat 用 D:\download\python.exe 启动
- 系统 Python 依赖已装齐：PySide6 6.11.1 + opencv + numpy + httpx + pyqtgraph

## 检测端打不开排查（第二轮）
- 代码逐步骤诊断通过：QApplication/Config/Client/LoginDialog 全 OK
- 根因：start.bat 换行符问题。Write 工具写 LF，Bash printf 转 CRLF 时反斜杠被 MSYS 转坏
- 最终修复：Write 写正确内容 + PowerShell 转 CRLF（避免 Bash 转义）
- 注意：.bat 脚本 CRLF 转换必须用 PowerShell，不能用 Bash printf（路径转换坑）

## 网页版实时摄像头检测（CameraRecognitionDemo.vue）
- 用户诉求：检测功能要「在平台（浏览器）中打开」，不要桌面 PySide6 客户端
- 新增 CameraRecognitionDemo.vue（约 900 行）：getUserMedia 摄像头 + ROI 框选 + 逐帧颜色识别 + 30秒终点状态机 + 色相直方图 + 保存
- 复用图片识别已调好的颜色算法（白平衡白色参考法 + 自动聚焦 + 5×5分区采样 + 纯色相判定）
- 状态机对齐 Python endpoint_state_machine：IDLE→INITIAL(酒红)→NEAR_ENDPOINT(蓝紫)→CANDIDATE_ENDPOINT(纯蓝计时)→ENDPOINT(30秒稳定)；返色取消计时；连续10帧UNKNOWN取消候选计时
- 性能：rAF 循环 150ms 节流（约6-7fps），抓帧缩至 480 宽，video aspect-ratio 动态匹配
- 保存：detectMode=CAMERA，附 stableDurationSeconds + 逐秒采样数据（submitSamples 接口新增）
- StudentDashboardView 集成在最前，startExperiment 按钮改为 scrollIntoView 到摄像头组件
- 验证：vite build 通过（2270 modules）；Node 复刻状态机 15/15 通过

## 实验记录草稿机制（保存=草稿，学生主动提交）
- 需求：保存实验记录后需学生主动「提交」才上传到教师端
- 状态流转：DRAFT（保存草稿）→ SUBMITTED（学生提交）→ REVIEWED（教师批阅）
- 后端：
  - submitExperiment 保存改 DRAFT（不再直接 SUBMITTED，submittedAt 不设）
  - 新增 submitToTeacher 方法 + PUT /api/experiments/{id}/submit 接口（DRAFT→SUBMITTED，重复提交报错）
  - listExperiments / exportCsv：教师/管理员默认 ne DRAFT（排除草稿）
  - ReviewService.createReview 加校验：DRAFT 不能批阅
- 前端：
  - experiment.ts 加 submitExperimentToTeacher
  - 图片/视频/摄像头三组件保存 submitStatus 改 DRAFT，提示改「已保存为草稿」
  - StudentDashboardView 记录列表：草稿显示「提交」按钮 + 状态区分草稿/待批阅/已批阅
  - style.css 加 .review-tag.draft 和 .submit-button 样式
- 端到端验证通过：保存DRAFT→教师看不到→学生提交→教师可见→重复提交/批阅草稿被拒

## 视频识别不显示画面修复
- 现象：上传视频后只显示进度条，不显示画面
- 根因：.video-stage 用 display:inline-grid + video 只设 max-width/max-height（无明确尺寸），百分比循环引用导致 video 画面塌陷
- 修复：.video-stage 改 display:grid + width/height 100%；video 改 width/height 100% + object-fit:contain + 深色背景

## 视频黑屏（编码问题诊断）
- 现象：视频画面黑屏，但进度条/时长正常（loadedmetadata 能读到 duration）
- 判断：浏览器能读元数据但解不出画面 → 视频编码不兼容（H.265/HEVC 或 MOV 变体，Chrome 不支持）
- 修复：VideoRecognitionDemo 加 @error/@loadeddata 监听 + videoError 提示「编码不支持，转 H.264 MP4」
- 用户机器无 ffmpeg；转码可用格式工厂，或用户系统 Python opencv

## 学生端页面拆分（四页面）
- 需求：学生端功能原全堆在"学习概览"一个页面，拆分成 4 个独立页面
- 新建 views/student/ 下 4 个组件，删除旧 StudentDashboardView.vue：
  - StudentOverviewView（学习概览）：课程卡片 + 3 StatCard + 当前任务 + 最新检测结果
  - StudentTasksView（实验任务）：任务详情+要求 + 三个识别组件（图片→视频→摄像头）
  - StudentExperimentsView（实验记录）：记录表格 + 提交按钮 + 分页
  - StudentFeedbackView（教师反馈）：评分环 + 平均分 + 批阅详情列表 + TrendChart
- 路由 4 个 path 分别指向 4 个组件
- 后端：ReviewService.listReviews + ReviewController.list 加 experimentId 参数（供反馈页查评分）
- 坑：views/student/ 子目录 import 相对路径需 ../../（不是 ../）
- 验证：4 页面 200，review?experimentId 正常

## 收尾排查（对照需求文档，20:17）
- 核心 P0 全完成，大部分 P1 也完成（导出/日志/页面拆分/采样落库/草稿机制）
- 有后端无前端的缺口：
  - AUTH-05 改密码：后端 UserController.changePassword 已有，前端无入口
  - STU-07 学生端导出：后端 CSV 导出支持学生，前端「实验记录」页无导出按钮
- 数据已落库但前端无展示：STU-05 关键帧/HSV 曲线/状态时间线详情页（samples/events 接口已有）
- 教师端统计页 TeacherStatisticsView 是真实数据（getOverview），非 mock
- 部署类：切 MySQL、打包 exe（检测端）
- 待处理：视频黑屏（编码问题）

## 学生端导出实验记录（收尾项 #1）
- StudentExperimentsView 记录面板加「导出 CSV」按钮（复用教师端 handleExport blob 下载逻辑）
- exportExperiments() 不传参，后端按学生身份只导出自己的记录
- 验证：HTTP 200 text/csv，带 BOM，学生导出自己的记录正确

## 修改密码入口（收尾项 #2）
- 后端 changePassword 接口已有（PUT /api/users/change-password，旧密码校验），前端补 UI
- 前端：user.ts 加 changePassword API；MainLayout 顶部栏加「修改密码」按钮 + 手写 modal（element-plus 未全局注册，用 scoped 弹窗）
- AppIcon 补 lock 图标
- 改密成功后 auth.logout + 跳登录
- 验证：错误旧密码拒→正确改密→新密码登录→改回，全通过

## 实验详情页（收尾项 #3）
- 需求：STU-05/TEA-03 关键帧+HSV曲线+状态时间线详情展示（答辩亮点）
- 前端：新建 ExperimentDetailView.vue（/student/experiments/:id）
  - 基本信息卡片（检测模式/识别状态/匹配度/HSV/三色占比/稳定时长/提交状态）
  - HSV 采样曲线（echarts 双 y 轴：hue 0-360 + S/V 0-1，markLine 标 235/315 阈值边界）
  - 状态时间线（CSS 时间轴）
  - 关键帧（downloadFile blob + createObjectURL 显示）
- experiment.ts 加 getSamples/getEvents/downloadFile + ColorSampleRecord/StateEventRecord/ExperimentFileRecord 类型
- 记录列表「查看详情」按钮跳转详情页（goDetail）
- 后端权限校验：ExperimentService 加 checkAccess + getExperimentDetail，samples/events/files 接口均校验学生只能访问自己的（NFR-SEC-04）
- 验证：详情 200、权限 403、采样/事件落库回读正常

## 文档同步（收尾项 #7）
- docs/开发进度与接口文档.md 严重过时（还写"检测端未开始"），全面更新：
  - 整体进度：检测端✅、前端24页、后端30+ API
  - 项目结构加 ColorSample/StateEvent 实体 + log 模块
  - API 清单加 submit/export/samples/events 接口
  - 前端状态：四页面+详情页+实时检测；待完成只剩部署类
  - 后续计划：四阶段全✅，第四阶段🟡进行中
- MySQL 仍未安装（切库需用户先装 MySQL 服务器）

## 检测端打包 exe（收尾项 #5）
- PyInstaller 6.22.0 已装（系统 Python D:\download\python.exe），依赖齐全（PySide6 6.11.1/opencv5/numpy2.2.2/httpx/pyqtgraph0.14）
- 打包命令：`D:/download/python.exe -m PyInstaller --noconsole --onefile --name titration-detector main.py`
- 产物：detector-client/dist/titration-detector.exe（153M，单文件）
- 验证：offscreen 模式运行退出码 124（启动成功保持运行，无崩溃无报错）
- .gitignore 加 detector-client/build/、dist/、*.spec
- 无害警告：pyqtgraph.opengl 缺 OpenGL、scipy._cdflib 隐藏导入（检测端不依赖）

## HSV 曲线不显示修复 + 演示数据补充
- bug：详情页 renderChart 在 samples 赋值后立即调用，但 v-if 的 chartEl DOM 未渲染（Vue 异步更新）→ renderChart 前 await nextTick()
- 演示数据：data.sql 补 color_samples（16条，体现酒红340→蓝紫245→纯蓝210变化）+ state_events（11条），3条演示实验都能展示曲线和时间线

## 学生上传图片存档 + 教师查看
- 需求：保存数据包含学生上传的图片，提交后教师能看到
- 前端：experiment.ts 加 uploadFile（FormData multipart）；ImageRecognitionDemo 保存 sourceFile 并在保存后 uploadFile(file, expId, 'SOURCE_IMAGE')
- 教师端 TeacherExperimentsView 详情弹窗加「学生上传的图片」：openDetail 调 getExperimentFiles + downloadFile 加载图片展示
- 后端 FileService.upload bug 修复：transferTo 相对路径解析到 Tomcat 临时目录导致 500 → 改用 Paths.get().toAbsolutePath() + Files.copy(InputStream)
- 验证：上传→学生查→教师查→教师下载全通过

## 前端按钮样式统一
- 用户反馈按钮布局不协调，全选四个区域统一调整
- 圆角统一 6px（primary-button 5px、camera-toggle 8px 都改 6px）
- 高度统一 38px（upload-button 42px→38px）
- camera-toggle 去掉 #3b6cb4 纯色覆盖，复用 upload-button 蓝色渐变
- btn-logout 边框 #e0e4e8→#dde1e6 + padding 统一

## 视频识别布局修复
- 用户反馈视频识别文字显示不全、布局不协调
- .video-frame-stats 4列→2列（4列在窄卡片每格仅73px，「整段酒红倾向」6字换行/截断）；strong 24→21px，div 改 flex column + span nowrap
- .video-current-status 防溢出：div 加 min-width:0、b 加 word-break、strong/small nowrap、padding 12px 16px
- .video-live-ratios label 列宽 74/48→66/46 + span/b nowrap
- .video-demo-header 标题区 flex:1+min-width:0（防挤压按钮）、span 加 line-height 1.6、align-items flex-start
- .video-analyze-button 高度 42→38px（对齐 upload-button）
- .video-demo-body 列宽 minmax(520,1.45fr)/minmax(360,.85fr) → minmax(500,1.35fr)/minmax(380,1fr)，右卡片更宽

## 视频识别显示不全（第二轮排查，用 Playwright 实测）
- 用 Playwright(msedge) 实测多视口(1000~1600px)，初始/模拟分析后状态均无文字裁剪，排除 CSS overflow 裁剪
- 真正根因1：.frame-timeline 61 个抽帧色块固定 flex 0 0 9px，scrollW 747 > clientW 361，横向滚动导致"显示不全"
  - 修复：色块改 flex 1 1 0 + min-width 2px + max-width 14px + overflow hidden，全部自适应可见（scrollW==clientW 361）
- 真正根因2：未分析状态 b 显示"等待播放 0.0%"（语义冗余）→ 改 currentFrame ? `${label} ${Math.round(conf)}%` : '等待播放'，confidence 去 .0 小数
- 顺带：.video-frame-stats strong line-height 1.2→1.1 消除 1px 亚像素溢出
- Playwright 检测脚本思路：登录→跳 /student/tasks→改 DOM 模拟分析后状态→遍历叶子文字节点查 scrollW>clientW

## 视频识别右侧文字字号比例调整
- 用户反馈右侧文字区域"文字页面比例不对"
- 根因：字号阶梯失衡——状态 strong 23px、统计数值 21px 只差 2px 且都偏大，与 13px 标签差距过大（1.6倍）
- 调整：状态 strong 23→20px；frame-stats strong 21→17px + span 13→12px
- 形成清晰阶梯：20（状态）> 17（数值）> 16（正文b）> 14（进度/消息）> 13（small）> 12（标签）

## 视频识别右侧"文字离右边界过近/显示不全"排查（未修改）
- 用户澄清：不是字号，是显示区域比例——文字离右侧边界过近且显示不全
- Playwright 精确测量 5 视口(1200~1600)+初始/分析后状态，测量各文字元素右边缘与 result-card 右边界的 gap
- 结论：无技术溢出（无 scrollW 溢出、result-card 内无 overflow:hidden 裁剪文字的容器）
  - 文字离右边界 30~35px（= 18px 卡片 padding + 12~16px 内层 padding），recognition-message 元素边缘最小 18px
  - result-card 双列布局下偏窄：1600→500px、1440→432、1366→401、1280→380（minmax 下限）
- 疑似原因：result-card 太窄塞 7 块内容（状态块+4格统计+3条占比+消息+按钮+帧时间线），内容拥挤视觉贴边
- 待确认方案：grid 列宽 1.35fr:1fr → 1.2fr:1fr 加宽右侧；或 ≤1400px 提前单列

## 图片识别渲染完整性检查+修复
- 用户要求：所有控件/文字/边框完全渲染、底部元素不被任务栏遮挡、无裁剪无遮挡
- Playwright 检测（1366×900）：0 文字溢出、预览卡 overflow:hidden 无裁剪、滚动容器是 .page-content（非 document）
- 修复1：直方图 canvas 内部分辨率 560px vs 显示 381px 被压缩 → drawHistogram 里 canvas.width/height 匹配 clientWidth/clientHeight
- 修复2：.page-content padding 20px 24px → 20px 24px 48px（底部留白，滚到底后最后模块余量 64px→更大）
- 关键认知：滚动容器是 .page-content（.main-area overflow hidden + .page-content overflow-y auto），检测脚本要用 pc.scrollTop 而非 window.scrollTo
